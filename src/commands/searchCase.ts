import {dataService} from "../dataService.js";


export async function searchCase(term: string) {
  // const allCases = await db.select().from(cases);
  // const results = allCases.filter((c) =>
  //   fuzzySearch(term.toLowerCase(), `${c.vin} ${c.name} ${c.surname}`.toLowerCase())
  // );

  // console.log(results.length > 0 ? results : "No matches found.");
}
